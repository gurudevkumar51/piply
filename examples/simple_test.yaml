version: "1"
title: Simple Piply Example
workspace: ../piply-demo

pipelines:
  simple_test_pipeline:
    title: Simple Test Pipeline
    description: Minimal two-step pipeline using current Piply YAML.
    tasks:
      step1:
        type: python
        path: pipelines/extract.py
        function: extract_data

      step2:
        type: cli
        command: python -c "print('Step 2 completed')"
        depends_on: [step1]
